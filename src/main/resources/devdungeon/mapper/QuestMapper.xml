<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="devdungeon.mapper.QuestMapper">
    <insert id="insertQuestWithChapter" useGeneratedKeys="true" keyProperty="id" parameterType="devdungeon.domain.QuestVO">
        INSERT INTO quest(title, content, author, reg_time, chapter_id)
        VALUES(#{title}, #{content}, #{author}, #{regTime}, #{chapterId})
    </insert>
</mapper>